// 221221 18:10
// redux toolkit 기본 세팅
// index.js에서 <Provider store={store}></Provider> 로 감싸기
import { configureStore } from "@reduxjs/toolkit";

export default configureStore({
    reducer: {},
});
